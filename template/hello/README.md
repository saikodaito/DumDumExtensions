A tiny example extension that shows what the `dd` API can do.

- Settings page built with the app's own controls
- Toast when you open a chat
- Modal that reads the open chat
- Storage that survives restarts (and stays out of backups)

Source: [DumDumExtensions](https://github.com/saikodaito/DumDumExtensions)
